/*h) Escreva um algoritmo que receba o número da conta e o saldo de várias pessoas. O
algoritmo deve imprimir todas as contas, os respectivos saldos e uma das mensagens:
positivo/negativo. Ao final, o percentual de pessoas com saldo negativo deve ser informado. O
algoritmo acaba quando se digita o número da conta igual a 0 (zero). */
const prompt = require("prompt-sync")();