/* c) Altere o algoritmo anterior para que, além da nota, também seja armazenado o nome do
aluno.
Para isso, um segundo vetor deve ser criado. Posteriormente, exibir um relatório das notas iguais
ou superiores a 7.0 e o nome do aluno com essa nota.*/
const prompt = require("prompt-sync")();